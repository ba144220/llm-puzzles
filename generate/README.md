In this folder, we will try to reproduce the `model.generate` function in Hugging Face.

We will measure the performance by
- the number of tokens generated per second
- the latency of generating a single token
- the peak memory usage
